# Node modules and build output
node_modules/
uploads/

# Local environment variables
.env

# System files
.DS_Store

# Logs or temp files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OAuth token (not used with Service Account, but safe to exclude)
token.json

# ❗ INCLUDE service-account key file
# If you named it 'service-account.json' or 'credentials.json', DO NOT ignore it!
# For safety, whitelist it explicitly if needed:
!service-account.json
!credentials.json
